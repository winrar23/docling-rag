embedding_model: all-MiniLM-L6-v2
chunk_size: 3200        # символы (≈800 токенов × 4 символа/токен)
chunk_overlap: 320      # символы (≈80 токенов = 10% от chunk_size)
top_k_results: 5
data_dir: data
log_file: logs/search.log
